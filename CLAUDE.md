# thinkts-saas

Monorepo：框架 + CLI + 插件 + 产品。目标是把 thinkts 搞成插件系统。

- `packages/thinkts/` — 框架引擎。抽象底层，上层只写业务
- `packages/thinkts-cli/` — 命令行工具
- `packages/*/` — 通用 SaaS 插件
- `apps/iotbiz/` — 共享设备支付平台

改代码前读 GOAL.md 和 RULES.md。
