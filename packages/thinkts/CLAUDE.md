# ThinkTS — Bun-native TypeScript Server Framework

## Quick context
DSL-driven SaaS runtime on Bun. See workspace-level `CLAUDE.md` for full project context.
Key modules: kernel (strategy pipeline), model (fluent query builder), dsl-model (JSON-driven CRUD), router.

## Commands
```bash
bun test              # 90 tests must pass
cd ../iotbiz/api && bun development.ts  # verify reference app works
```

## Rules
See `RULES.md` in workspace root.
