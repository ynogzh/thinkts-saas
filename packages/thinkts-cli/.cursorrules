# ThinkTS CLI 编码规则

## 模板质量
- templates/ 是用户的第一印象 — 代码必须简洁、有注释、无硬编码。
- 模板中的 `.env` 只能有占位值，不能有真实凭证。
- 每个模板变更后必须用 `thinkts new test-app` 验证脚手架可用。

## 代码风格
- 生成器输出必须稳定可预测（JSON key 顺序一致，缩进统一）。
- CLI 命令必须打印清晰的进度日志，让用户知道生成了什么。

## 验证
- 修改后 MUST 运行 `bun test`（5 tests）。
- 修改生成逻辑后 MUST 在 iotbiz 中重新生成并对比差异。
